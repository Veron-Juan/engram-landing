---
title: HTTP API
description: Use Engram’s local HTTP API for plugins, integrations, and automation that prefer JSON over stdio.
sidebar:
  order: 4
---

The HTTP API mirrors the same model exposed elsewhere.

Core route groups:

- health
- sessions
- observations
- search
- timeline
- prompts
- context
- export and import

By default, the server listens on `127.0.0.1:7437`.