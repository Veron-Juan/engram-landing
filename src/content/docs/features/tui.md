---
title: Terminal UI
description: Browse memories interactively through Engram’s Bubbletea-based TUI.
sidebar:
  order: 5
---

Launch the TUI with:

```bash
engram tui
```

Key behaviors:

- vim-style movement with `j` and `k`
- drill into detail with `Enter`
- search with `/`
- open timeline context with `t`

This surface is optimized for inspection, not just automation.