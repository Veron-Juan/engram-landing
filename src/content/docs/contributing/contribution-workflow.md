---
title: Contribution Workflow
description: Open an issue, get approval, submit a focused PR, and satisfy the validation rules.
sidebar:
  order: 2
---

The workflow is strict on purpose:

1. open an issue
2. wait for `status:approved`
3. open a PR linked with `Closes #N`
4. apply exactly one `type:*` label

Do not start with a PR and hope the repo will adapt around it.