---
title: OpenCode Plugin
description: The OpenCode plugin layers session orchestration and prompt shaping on top of Engram’s MCP tools.
sidebar:
  order: 4
---

The OpenCode plugin is a thin TypeScript layer.

It auto-starts the server, creates sessions, injects protocol guidance, and helps local-model setups keep a compatible system-message shape.